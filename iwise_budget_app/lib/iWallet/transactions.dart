import 'package:flutter/material.dart';

//To make our code easier to debugg and navigate through,
//We create a Transaction file that handles all our transactions
//With records from the database
class Transactions extends StatelessWidget {
//  final String transactionType;
//  final double transactionAmount;
//  Transactions(this.transactionType, this.transactionAmount);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Expanded(
        child: Row(
          children: <Widget>[],
        ),
      ),
    );
  }
}
